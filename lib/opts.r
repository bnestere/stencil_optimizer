stencil_clioptions = list(
                          make_option(c("--in"), type="character", default=NULL, 
                                      help="Input stencil to analyze",
                                      metavar="character")
                          )
